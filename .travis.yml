language: java
jdk: oraclejdk8

stages:
  - Compile backend
  - Backend test

before_install:
  - mvn -f backend/pom.xml

install:

  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
  - npm install

jobs:
  - include:
    - script: mvn test -B